import 'package:flutter_test/flutter_test.dart';

void main() {
  // Widget tests require a live Supabase instance; run integration tests instead.
  testWidgets('placeholder', (tester) async {
    expect(true, isTrue);
  });
}
